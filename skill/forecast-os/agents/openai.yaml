interface:
  display_name: "ForecastOS"
  short_description: "Search prediction markets and run human-approved ForecastOS workflows"
  default_prompt: "Use $forecast-os to search prediction-market context, avoid guessing future-event probabilities, draft markets, and advance human-approved ForecastOS workflows."

policy:
  allow_implicit_invocation: true
